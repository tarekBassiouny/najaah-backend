name: Sync main to dev

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Get latest tag
        id: tag
        run: |
          tag=$(git describe --tags --abbrev=0)
          echo "tag=$tag" >> $GITHUB_OUTPUT

      - name: Get release notes
        id: notes
        run: |
          notes=$(curl -s \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            https://api.github.com/repos/${{ github.repository }}/releases/tags/${{ steps.tag.outputs.tag }} \
            | jq -r '.body')

          echo "notes<<EOF" >> $GITHUB_OUTPUT
          echo "$notes" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create sync branch
        run: |
          git checkout -B sync-main-to-dev
          git push origin sync-main-to-dev --force

      - name: Create PR main → dev
        id: cpr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: dev
          branch: sync-main-to-dev
          title: "🔄 Sync main → dev after release ${{ steps.tag.outputs.tag }}"
          body: |
            ## 🔄 Automated Sync PR
            **Release:** ${{ steps.tag.outputs.tag }}

            ### 📦 Release Notes
            ${{ steps.notes.outputs.notes }}

            ---
            This PR ensures that **dev** stays ahead of **main** after a tagged release.
          labels: "automation,sync"
          delete-branch: false
          draft: false

      - name: Auto-approve the PR
        if: ${{ steps.cpr.outputs.pull-request-number }}
        uses: hmarr/auto-approve-action@v3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Enable auto-merge
        if: ${{ steps.cpr.outputs.pull-request-number }}
        uses: peter-evans/enable-pull-request-automerge@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          pull-request-number: ${{ steps.cpr.outputs.pull-request-number }}
          merge-method: merge

      - name: Delete sync branch after merge
        if: ${{ steps.cpr.outputs.pull-request-number }}
        run: |
          sleep 10
          git push origin --delete sync-main-to-dev || true
