You are working in a Git repository with a `dev` branch.

SHARED SOURCE OF TRUTH (MANDATORY)
==================================
Before doing any work, read and follow:
1. `/docs/codex/CODEX_ORCHESTRATOR_ENTRY.md`
2. `/docs/codex/CODEX_CLAUDE_ALIGNMENT.md`
3. The mapped `.claude/agents/*.md` file for the task role you are emulating
4. The mapped `.claude/skills/**` files for the current phase
5. The active feature plan in `/docs/feature/`
6. The active progress tracker in `/docs/feature/*-progress.md`
7. Any active frontend contract docs in `/docs/contracts/`

Do not invent a separate Codex-only workflow when a `.claude` file already defines it.
Use the same phase review gate, planning flow, progress tracker, and contract docs as Claude.

TASK CONTEXT
============
Internal Task ID: {{TASK_ID}}
GitHub Issue Number: #{{ISSUE_ID}}
Task title: [LMS] {{TASK_TITLE}}

{{TASK_BODY}}

GIT WORKFLOW (MANDATORY)
=======================
1. Checkout `dev`
2. Pull latest changes from `origin/dev`
3. Prefer creating a dedicated git worktree for the task branch instead of coding in the main checkout
4. Create a new branch:
   feature/{{TASK_BRANCH}}
5. Implement ONLY this task
6. Commit changes with message:
   feat({{TASK_ID_LOWER}}): {{TASK_SLUG}} (#{{ISSUE_ID}})
7. Push branch to origin
8. Open a Pull Request targeting `dev` with:
   - Title:
     [LMS][{{TASK_ID}}][#{{ISSUE_ID}}] {{TASK_TITLE}}
   - Description:
     - Task: {{TASK_ID}}
     - GitHub Issue: #{{ISSUE_ID}}
     - Summary of changes
     - What was NOT changed
     - Tests added/updated
     - Link to issue #{{ISSUE_ID}}

STOP after creating the PR.

STRICT SAFETY RULES
===================
- Complete the phase review gate before starting implementation for any planned phase
- Prefer a separate git worktree for task execution when working on a planned phase or branch-scoped task
- Implement ONLY what is defined in this task
- Do NOT touch other tasks
- Do NOT refactor unrelated code
- Do NOT change APIs unless explicitly required
- Do NOT modify database schema unless specified
- If unsure → STOP and ask for clarification

ALLOWED ACTIONS
===============
- Create new files required for this task
- Modify existing files ONLY when required
- Add tests related ONLY to this task

FORBIDDEN ACTIONS
=================
- No speculative improvements
- No TODO placeholders
- No cross-task integration
- No follow-up tasks

OUTPUT REQUIREMENTS
===================
- All changes committed to the feature branch
- PR created and ready for review
- Stop execution immediately after PR creation
